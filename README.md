# KhoaCodeBug portfolio

Published portfolio: https://khoacodebug.github.io/

Static export of the Unity gameplay portfolio, including four short looping demo videos and English/Vietnamese resumes. GitHub Pages serves the root of `main`; `.nojekyll` preserves the `_next` assets.

This repository contains the generated deployment output. To update it, export the portfolio source again and replace the deployment files together.
